---
applyTo: "upr/**"
---

This path contains the internal logic for the randomizer which is a subtree for another project.

Rarely does the contents of the directory need to be inspected; it should be treated as a "black box" that the app has no knowledge about the inner workings of.

Changes to the source code in the module should be avoided to keep parity with the upstream release.
